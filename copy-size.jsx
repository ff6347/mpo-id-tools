(function (thisObj) {  /**   * This Script copies the size of one pageItem to another   */  main();  function main() {    if (app.activeDocument.selection.length < 2) {      alert("select 2 items");      return;    }    var srcitem = app.activeDocument.selection[0];    var srcgb = srcitem.geometricBounds;    for (var t = 1; t < app.activeDocument.selection.length; t++) {      var targetitem = app.activeDocument.selection[t];      var trggb = targetitem.geometricBounds;      var W = srcgb[3] - srcgb[1];      var H = srcgb[2] - srcgb[0];      var newgb = [trggb[0], trggb[1], trggb[0] + H, trggb[1] + W];      targetitem.geometricBounds = newgb;      targetitem.fit(FitOptions.CENTER_CONTENT);      targetitem.fit(FitOptions.PROPORTIONALLY);    }    srcitem.select(SelectionOptions.REMOVE_FROM);  }})(this);